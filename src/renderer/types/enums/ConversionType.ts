/**
 * Type for possible conversions to be shown in the monitor.
 * Should not be used as an encoding type for SerialPort.
 */
export enum ConversionType {
  ASCII = 'Ascii',
  DEC = 'Dec',
  BIN = 'Bin',
  HEX = 'Hex',
}
